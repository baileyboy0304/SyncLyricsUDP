# ===========================================
# SyncLyrics Environment Variables
# ===========================================
# Most settings can be configured via settings.json or the Settings UI.
# Use this file for API credentials and Docker/HAOS path overrides.
# 
# Settings that use conf("category.setting") in config.py are converted as:
#   category.setting → CATEGORY_SETTING (uppercase, dots become underscores)
# 
# Examples:
#   debug.enabled → DEBUG_ENABLED
#   features.parallel_provider_fetch → FEATURES_PARALLEL_PROVIDER_FETCH
#   storage.cache.duration_days → STORAGE_CACHE_DURATION_DAYS
#
# Direct os.getenv() calls use the exact variable name (e.g., SPOTIFY_CLIENT_ID)
# ===========================================

# Important ENV Variables

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

## For best performance, host your own instances of https://github.com/akashrchandran/spotify-lyrics-api
# For example: https://fake-spotify-lyrics-api-azure.vercel.app

SPOTIFY_BASE_URL=

# SPOTIFY_REDIRECT_URI=

# Spotify OAuth needs HTTPS if not using localhost (http://127.0.0.1:9012/callback). So use https://your-ip:9013/callback for that. 

# SPOTIPY_CACHE_PATH=.cache

LASTFM_API_KEY=

FANART_TV_API_KEY=


# ===========================================
# ACRCloud (optional - audio recognition fallback)
# ===========================================
# ACRCloud is used as a fallback when Shazamio fails to identify a song.
# Get credentials from https://console.acrcloud.com/
# Without these, ACRCloud fallback is automatically disabled.

# ACRCLOUD_HOST=identify-eu-west-1.acrcloud.com
# ACRCLOUD_ACCESS_KEY=your_access_key_here
# ACRCLOUD_ACCESS_SECRET=your_access_secret_here
# ACRCLOUD_DAILY_LIMIT=100        # Max requests per day (default: 100)
# ACRCLOUD_COOLDOWN=30            # Seconds between ACRCloud requests (default: 30)



# ----- SERVER -----
# Session secret key (auto-generated and persisted to .quart_secret if not set)
# QUART_SECRET_KEY=your-secret-key

# Server port (default: 9012)
# SERVER_PORT=9012

# Custom Spotify Lyrics API (if self-hosting)
# SPOTIFY_BASE_URL=https://your-instance.vercel.app


# ----- HAOS / DOCKER PERSISTENCE -----
# For Home Assistant OS or Docker, set these to persistent paths
# In HAOS, use /config/* for persistence across addon restarts

# SYNCLYRICS_SETTINGS_FILE=/config/settings.json
# SYNCLYRICS_LYRICS_DB=/config/lyrics_database
# SYNCLYRICS_ALBUM_ART_DB=/config/album_art_database
# SYNCLYRICS_CACHE_DIR=/config/cache
# SPOTIPY_CACHE_PATH=/config/.spotify_cache
# SYNCLYRICS_CERTS_DIR=/config/certs

# ----- ADVANCED: Debug & Feature Overrides -----
# These are better configured via Settings UI (http://localhost:9012/settings)
# Only use ENV vars if you cannot access the UI

# DEBUG_ENABLED=false
# DEBUG_LOG_LEVEL=INFO
# FEATURES_PARALLEL_PROVIDER_FETCH=true
# STORAGE_CACHE_ENABLED=true
